      <%= form_for genre do |f| %>
        <div class="movie__field">
          <%= f.label :name, 'Gênero', class: "form-label" %>
          <%= f.text_field :name, class: "form-control" %>
        </div>
        <div class="movies__btn-add">
          <%= f.submit 'Salvar', class: "btn btn-success" %>
        </div>
      <% end %>